{
  "//": "Dev-only. The Angular dev server proxies API paths to the docker-compose backends, so the browser sees a single same-origin host (no CORS). Mirrors how prod routes dev-auth.* / dev-api.* subdomains — see CLAUDE.md 'AWS Environment'.",
  "/auth": {
    "target": "http://localhost:3001",
    "secure": false,
    "changeOrigin": true
  },
  "/profiles": {
    "target": "http://localhost:3002",
    "secure": false,
    "changeOrigin": true
  }
}
